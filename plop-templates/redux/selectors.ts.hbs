import { RootState } from '../../store.types';
import { State } from './{{kebabCase redux_module_name}}.types';

export const getCurrentState = (state: RootState): State => state.{{camelCase redux_module_name}};
