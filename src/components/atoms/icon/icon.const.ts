import {
  HeartIcon,
  SearchIcon,
  XIcon,
  HomeIcon,
} from '@heroicons/react/outline';

export const ICONS = {
  SearchIcon,
  HeartIcon,
  XIcon,
  HomeIcon,
};

export const ICONS_NAMES = Object.keys(ICONS) as Array<keyof typeof ICONS>;
